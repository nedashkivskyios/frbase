import React from 'react';
import {AppBar, Toolbar} from "@mui/material";

export const NavBar = () => {
  return (
    <AppBar>
      <Toolbar variant={"dense"}>
        FireBase Example
      </Toolbar>
    </AppBar>
  );
};
